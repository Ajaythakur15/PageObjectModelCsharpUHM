trigger:
- master    # change to main if needed

pool:
  vmImage: 'windows-latest'

steps:
# Restore NuGet packages
- task: DotNetCoreCLI@2
  displayName: 'Restore'
  inputs:
    command: 'restore'
    projects: 'PageObjectModelCsharp.csproj'

# Build the project
- task: DotNetCoreCLI@2
  displayName: 'Build'
  inputs:
    command: 'build'
    projects: 'PageObjectModelCsharp.csproj'
    arguments: '--configuration Release'

# Run tests but DO NOT fail the job if some tests fail
- task: DotNetCoreCLI@2
  displayName: 'Test'
  continueOnError: true                    # <-- important
  inputs:
    command: 'test'
    projects: 'PageObjectModelCsharp.csproj'
    arguments: >
      --configuration Release
      --logger "trx;LogFileName=TestResults.trx"
      --collect "XPlat Code Coverage"
    publishTestResults: false

# Publish TRX so Tests tab shows results
- task: PublishTestResults@2
  displayName: 'Publish test results'
  condition: succeededOrFailed()          # run even if tests failed
  inputs:
    testResultsFormat: 'VSTest'
    testResultsFiles: '**/TestResults.trx'
    searchFolder: '$(System.DefaultWorkingDirectory)'
    mergeTestResults: true
    failTaskOnFailedTests: false          # don't fail this step
    testRunTitle: 'UI Automation - $(Build.BuildNumber)'

# Publish code coverage
- task: PublishCodeCoverageResults@2
  displayName: 'Publish code coverage'
  condition: succeededOrFailed()          # run even if tests failed
  inputs:
    codeCoverageTool: 'Cobertura'
    summaryFileLocation: '$(System.DefaultWorkingDirectory)/**/coverage.cobertura.xml'
    reportDirectory: '$(System.DefaultWorkingDirectory)/**/TestResults'
    failIfCoverageEmpty: false
